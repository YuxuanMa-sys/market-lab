# 复制为 .env 后填入。所有 key 都是可选的：没有 key 时自动用免费数据源(yfinance)。
# 注意：.env 已在 .gitignore 里，永远不要提交到 git（上次 market-dashboard 的教训）。

# Polygon.io —— 行情数据（开通后行情自动切到 Polygon）
POLYGON_API_KEY=

# Ortex —— 实时空头数据（做空比例、借券费率、utilization）
ORTEX_API_KEY=

# Finnhub —— 新闻/财报日历（免费注册即可）
FINNHUB_API_KEY=
